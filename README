## How to Run

To execute the script, run the following command in your terminal:

```bash
python calculate_sessions.py <path_to_input_file>
```

## Limitations

- The script only accepts a single file path as an argument.
- The input file is expected to be a text-based log file.
